global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: ekrs-rag
    static_configs:
      - targets: ['rag:9090']    # 容器内互访走 docker network DNS
    metrics_path: /metrics
