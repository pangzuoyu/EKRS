# Phase 5.5 D: `/metrics` 主端口下线 → Sidecar Exporter — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 Prometheus `/metrics` 从 RAG 主端口 (:8000) 移到 sidecar (:9090),同时加 prometheus + scrape config 让开发环境真的能拉到指标。

**Architecture:** 主 FastAPI app 不再注册 `routes/metrics`;lifespan 起 `start_http_server(9090, 127.0.0.1)`,关停 `shutdown()+server_close()`;`PROMETHEUS_MULTIPROC_DIR` 可选(多 worker 才需,部署层设);docker-compose 加 prometheus 服务拉 rag:9090。

**Tech Stack:** prometheus_client `start_http_server` / `multiprocess.MultiProcessCollector`;FastAPI lifespan;`socket` 取 free port;docker-compose + prom/prometheus image。

**Spec:** `docs/superpowers/specs/2026-07-13-phase5.5-metrics-exporter-design.md` (385 行, P1 已修)

---

## Global Constraints

- 严格按 spec 5 设计决策 D1-D5。详见 spec §42-50。
- PROMETHEUS_MULTIPROC_DIR 可选;env 必须在 uvicorn fork 前由部署层设(lifespan 内 set 已晚)。
- 不改 Iron Rules R1-R7。
- 不改 15 个 audit event schema。
- 不改 Phase 5 replay 行为。
- 不动 `_isolate_prometheus_registry` session-scope,但加 multiproc 模式清理(`prometheus_multiproc_*` collectors)。
- TDD:每 task 先写 failing test → 实现 → pass → commit。
- 每 task 单 commit(message 包含 task 编号前缀,如 `T3:`)。
- 工作目录: `cd rag && ...`(除 docker-compose 任务)。

---

## File Inventory

| 类型 | 路径 |
|---|---|
| Delete | `rag/ekrs_rag/api/routes/metrics.py` (16 行) |
| Delete | `rag/tests/integration/test_metrics_endpoint.py` (84 行) |
| Modify | `rag/ekrs_rag/main.py` (lifespan 段,~12 行增 + 2 行减) |
| Modify | `.env.example` (+3 行, -1 行) |
| Modify | `rag/tests/conftest.py` (+1 行 multiproc 清理循环) |
| Create | `rag/tests/integration/test_metrics_exporter.py` (~80 行,3 测试 + helper) |
| Modify | `deployment/docker-compose.yml` (+17 行 prometheus service) |
| Create | `deployment/prometheus.yml` (~12 行 scrape config) |

净变化 ≈ +120 / -90 行。

---

### Task 1: 删 routes/metrics.py + test_metrics_endpoint.py(原子清理)

**Files:**
- Delete: `rag/ekrs_rag/api/routes/metrics.py`
- Delete: `rag/tests/integration/test_metrics_endpoint.py`

- [ ] **Step 1: 确认两个文件存在且 baseline 测试通过**

```bash
cd /home/pangzy/code_project/EKRS
ls rag/ekrs_rag/api/routes/metrics.py rag/tests/integration/test_metrics_endpoint.py
cd rag && python -m pytest tests/integration/test_metrics_endpoint.py -v --tb=short 2>&1 | tail -10
```

Expected: 两个文件存在;原 3 测试通过(route 还在,grep "3 passed")。

- [ ] **Step 2: 删除两个文件**

```bash
cd /home/pangzy/code_project/EKRS
git rm rag/ekrs_rag/api/routes/metrics.py
git rm rag/tests/integration/test_metrics_endpoint.py
```

- [ ] **Step 3: 跑测试基线 → 期望 FAIL(`routes/metrics` 在 main.py 还引用着)**

```bash
cd rag && python -m pytest tests/integration/test_metrics_endpoint.py -v --tb=short 2>&1 | tail -5
```

Expected: `no such file` 或 `ModuleNotFoundError`(因为 main.py:19 还有 `from .api.routes import ... metrics` import)。这个 fail 是预期的,T2-T6 实施 lifespan 后会修复。

注:如果 main.py 也立刻报错,提交前先临时 stash,git log 完不要保留此 fail 状态。先 `git stash` 或在 main.py 临时注释掉 `from .api.routes import ... metrics` 那行(后续 Task 4 正式删)。

- [ ] **Step 4: Commit 删文件**

```bash
cd /home/pangzy/code_project/EKRS
git commit -m "T1: delete routes/metrics.py + test_metrics_endpoint.py

- rag/ekrs_rag/api/routes/metrics.py: D 计划从主端口下线
- rag/tests/integration/test_metrics_endpoint.py: 由 T3 重写为 test_metrics_exporter.py 测 sidecar
- main.py:19 暂时保留 metrics import(T4 Task 删);测试套件会在 Task 4 后重新绿"
```

---

### Task 2: conftest 加 multiproc collector 清理

**Files:**
- Modify: `rag/tests/conftest.py` (在 `_isolate_prometheus_registry` 末尾加循环)

- [ ] **Step 1: 阅读现有 conftest 加 multiproc 清理块**

```bash
cd /home/pangzy/code_project/EKRS
cat rag/tests/conftest.py
```

确认现有结构:`yield` 后循环 `to_remove` 按 `rag_*` 名前缀清理 collectors。

- [ ] **Step 2: 加 multiproc collector 清理**

修改 `rag/tests/conftest.py`:

```python
    # ... existing to_remove loop ...
    for c in to_remove:
        try:
            REGISTRY.unregister(c)
        except KeyError:
            pass

    # Phase 5.5 D: also clean up multiproc-mode collectors. MultiProcessCollector
    # (registered via lifespan when PROMETHEUS_MULTIPROC_DIR is set) prefixes
    # its child collectors with 'prometheus_multiproc_'. Without this, repeated
    # sessions accumulate duplicates.
    multiproc_to_remove = []
    for collector in list(REGISTRY._collector_to_names.keys()):
        names = REGISTRY._collector_to_names.get(collector, set())
        if any(n.startswith("prometheus_multiproc_") for n in names):
            multiproc_to_remove.append(collector)
    for c in multiproc_to_remove:
        try:
            REGISTRY.unregister(c)
        except KeyError:
            pass
```

新加 12 行(精确算:`multiproc_to_remove = []` 块 + 注释)。原文件 37 行 → ~49 行。

- [ ] **Step 3: 跑测试 → 不回归**

```bash
cd rag && python -m pytest tests/ -q --tb=short 2>&1 | tail -5
```

Expected: 测试通过数与之前持平(可能因为 Task 1 删文件略减)。**1 skipped, 0 failed** 必须(1 skipped 是之前的 pre-existing skip)。

- [ ] **Step 4: Commit**

```bash
cd /home/pangzy/code_project/EKRS
git add rag/tests/conftest.py
git commit -m "T2: conftest._isolate_prometheus_registry cleans prometheus_multiproc_* collectors

MultiProcessCollector (registered by lifespan when PROMETHEUS_MULTIPROC_DIR set)
prefixes its child collectors 'prometheus_multiproc_*'. Without cleanup, repeated
sessions accumulate Duplicate timeseries warnings. Adds symmetric loop to existing
rag_* cleanup. No behavior change when multiproc mode is not active."
```

---

### Task 3: tests/integration/test_metrics_exporter.py 整套(failing first)

**Files:**
- Create: `rag/tests/integration/test_metrics_exporter.py`

**Interfaces:**
- Consumes: `free_port()` helper(此 task 内定义)、`create_app()` 触发 lifespan、`safe_inc` 增 counter、`httpx.get()` 拉 sidecar、`ObservabilityMiddleware`。
- Produces: 3 测试 + free_port + 全部用 env vars(`PROMETHEUS_MULTIPROC_DIR`、`METRICS_HOST`、`METRICS_PORT`)动态配 9090 sidecar。

- [ ] **Step 1: 创建 test_metrics_exporter.py 文件**

```python
"""Phase 5.5 D: Sidecar metrics exporter on :9090 (port from env).

The RAG main FastAPI app on :8000 no longer exposes /metrics. A sidecar wsgi
HTTP server runs on `METRICS_PORT` (default 9090) bound to `METRICS_HOST`
(default 127.0.0.1). Prometheus scrapes the sidecar, not the main app.

These tests:
  1. /metrics on the sidecar returns valid Prometheus exposition format
     (HELP / TYPE lines).
  2. Port comes from METRICS_PORT env var (not hard-coded).
  3. Lifespan teardown releases the port (no TIME_WAIT bleed across runs).
"""
import os
import socket

import httpx
import pytest

from ekrs_rag.main import create_app
from ekrs_rag.observability.metrics import METRICS, safe_inc


def free_port() -> int:
    """Bind ephemeral port, return its number. SO_REUSEADDR avoids TIME_WAIT bind errors."""
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.setsockopt(socket.SOL_SOCKET, socket.SO_REUSEADDR, 1)
    s.bind(("127.0.0.1", 0))
    port = s.getsockname()[1]
    s.close()
    return port


@pytest.fixture
def sidecar_env(tmp_path, monkeypatch):
    """Configure env for sidecar exporter: free port + multiproc dir."""
    port = free_port()
    multiproc = tmp_path / "prom"
    multiproc.mkdir()
    monkeypatch.setenv("METRICS_HOST", "127.0.0.1")
    monkeypatch.setenv("METRICS_PORT", str(port))
    monkeypatch.setenv("PROMETHEUS_MULTIPROC_DIR", str(multiproc))
    return {"port": port, "multiproc": multiproc}


def test_exporter_serves_prometheus_format(sidecar_env):
    """GET 127.0.0.1:<port>/metrics returns 200 with valid Prometheus content."""
    app = create_app()
    with app.router.lifespan_context(app):
        # Lifespan has started; exporter on sidecar_env['port']
        url = f"http://127.0.0.1:{sidecar_env['port']}/metrics"
        resp = httpx.get(url, timeout=2.0)
        assert resp.status_code == 200
        assert "text/plain" in resp.headers["content-type"]
        body = resp.text
        assert "# HELP rag_ingestion_total" in body
        assert "# TYPE rag_ingestion_total counter" in body


def test_exporter_listens_on_configured_port(sidecar_env):
    """Port comes from METRICS_PORT env, not hardcoded 9090."""
    app = create_app()
    with app.router.lifespan_context(app):
        # Manually issue a request to the dynamically-allocated port.
        url = f"http://127.0.0.1:{sidecar_env['port']}/metrics"
        resp = httpx.get(url, timeout=2.0)
        assert resp.status_code == 200
        # Contradiction check: if exporter were on hardcoded 9090, this port would
        # fail. Asserting the configured port works AND has Prometheus content
        # proves the port is dynamic.
        assert sidecar_env["port"] != 9090 or os.environ.get("FORCE_HARDCODED") != "1", (
            "fix the test: free_port() occasionally returns 9090; the assertion"
            " above is a sanity guard, real verification is the resp 200"
        )


def test_exporter_stops_on_lifespan_exit(sidecar_env):
    """After lifespan teardown, the port is no longer reachable."""
    app = create_app()
    with app.router.lifespan_context(app):
        # Confirm exporter is alive
        url = f"http://127.0.0.1:{sidecar_env['port']}/metrics"
        resp = httpx.get(url, timeout=2.0)
        assert resp.status_code == 200

    # Lifespan has exited; server_close() should have released the socket
    with pytest.raises((ConnectionRefusedError, httpx.ConnectError, OSError)):
        httpx.get(url, timeout=1.0)
```

保存到 `rag/tests/integration/test_metrics_exporter.py`。file ~80 行。

- [ ] **Step 2: 跑测试 → 期望 FAIL**

```bash
cd rag && python -m pytest tests/integration/test_metrics_exporter.py -v --tb=short 2>&1 | tail -15
```

Expected: 3 个测试全部 FAIL。原因(预期):
- `create_app()` 触发 lifespan,但 lifespan 还没有 start_http_server 调用(主端口仍暴露 /metrics)。
- `app.state.metrics_httpd` 不存在;sidecar exporter 没起来。
- `free_port` 函数本身可工作(这是 helper,单独测)。

若 main.py:19 还有 `from .api.routes import ... metrics` import 而 `routes/metrics.py` 已删,会先抛 `ModuleNotFoundError` — 需先在 main.py 临时注释掉该 import(等 T4 正式删)。

- [ ] **Step 3: Commit failing tests**

```bash
cd /home/pangzy/code_project/EKRS
git add rag/tests/integration/test_metrics_exporter.py
git commit -m "T3: write failing test_metrics_exporter.py with 3 tests + free_port helper

Tests:
  - test_exporter_serves_prometheus_format: 200 + Prometheus content-type
  - test_exporter_listens_on_configured_port: port from METRICS_PORT env
  - test_exporter_stops_on_lifespan_exit: socket released post-teardown

Helper: free_port() with SO_REUSEADDR.

Tests FAIL because lifespan does not yet call start_http_server (T4 implements).
This is the RED phase of TDD."
```

---

### Task 4: main.py lifespan 加 start_http_server + 删 routes/metrics 引用

**Files:**
- Modify: `rag/ekrs_rag/main.py` (顶部 imports + lifespan 段)

**Interfaces:**
- Consumes: env vars `PROMETHEUS_MULTIPROC_DIR`(可选)、`METRICS_HOST`(默认 127.0.0.1)、`METRICS_PORT`(默认 9090)。
- Produces: lifespan 启动 sidecar exporter,handle 存 `app.state.metrics_httpd`。

- [ ] **Step 1: 修改 main.py 顶部 imports**

```python
# rag/ekrs_rag/main.py
# 原第 19 行:
#   from .api.routes import constraints, ingestion, metrics
# 改为:
from .api.routes import constraints, ingestion
```

(同步移除 `metrics` import,因为文件已删。)

- [ ] **Step 2: 添加 prometheus_client imports**

在 main.py 顶部 imports 区(`import os` 之后或紧邻):

```python
from pathlib import Path
from prometheus_client import REGISTRY, multiprocess, start_http_server
```

(`Path` 已 import,无需重复。如未 import 加之。)

- [ ] **Step 3: 在 lifespan 开头加 sidecar 启动块**

在 `setup_logging(...)` 调用之前加:

```python
    # ---- Phase 5.5 D: sidecar metrics exporter ----
    # PROMETHEUS_MULTIPROC_DIR 可选。单 worker 模式(开发)留空即可。
    # 多 worker 模式必须在 uvicorn fork 子进程前由部署层设置
    # (docker-compose env / k8s Deployment env / shell export)。
    # 注:Counters 在 rag/ekrs_rag/observability/metrics.py:18 的 import-time
    # side-effects 中已注册,env var 检查仅为 fail-soft 提示,不在这里 setenv。
    multiproc_dir = os.environ.get("PROMETHEUS_MULTIPROC_DIR")
    if multiproc_dir:
        p = Path(multiproc_dir)
        p.mkdir(parents=True, exist_ok=True)
        multiprocess.MultiProcessCollector(REGISTRY)
        logger.info("Multi-process metrics mode: %s", p)

    metrics_host = os.environ.get("METRICS_HOST", "127.0.0.1")
    metrics_port = int(os.environ.get("METRICS_PORT", "9090"))
    httpd = start_http_server(metrics_port, addr=metrics_host)
    logger.info("Metrics exporter listening on %s:%d", metrics_host, metrics_port)
    app.state.metrics_httpd = httpd
```

约 12 行。

- [ ] **Step 4: 在 lifespan yield 之后加 shutdown + server_close**

找到原 yield 块:

```python
    yield

    logger.info("Shutting down EKRS RAG service")
```

改为:

```python
    yield

    # ---- Phase 5.5 D: sidecar shutdown ----
    httpd = getattr(app.state, "metrics_httpd", None)
    if httpd is not None:
        httpd.shutdown()        # 停止 serve_forever()
        httpd.server_close()   # 立即释放 listening socket(去 TIME_WAIT)
    logger.info("Metrics exporter stopped")

    logger.info("Shutting down EKRS RAG service")
```

约 7 行新增,顺序:`shutdown()` 先让 thread 退出,`server_close()` 释放 socket。

- [ ] **Step 5: 在 create_app() 中移除 metrics router 注册**

`rag/ekrs_rag/main.py:181-183`:

```python
    app.include_router(ingestion.router)
    app.include_router(metrics.router)     # 删除这行
    app.include_router(constraints.router)
```

改为:

```python
    app.include_router(ingestion.router)
    app.include_router(constraints.router)
```

- [ ] **Step 6: 跑 tests → 期望 test 1+2 PASS,test 3 still FAIL**

```bash
cd rag && python -m pytest tests/integration/test_metrics_exporter.py -v --tb=short 2>&1 | tail -10
```

Expected:
- `test_exporter_serves_prometheus_format` PASS(exporter 已起)
- `test_exporter_listens_on_configured_port` PASS
- `test_exporter_stops_on_lifespan_exit` 可能 FAIL 或 PASS 取决于 `server_close()` 是否真的及时释放 socket

若 test 3 FAIL 是因为 `httpx.ConnectError` 没抛(端口还没完全释放),进 T5 调整 wait 时长或加重试。

- [ ] **Step 7: Commit**

```bash
cd /home/pangzy/code_project/EKRS
git add rag/ekrs_rag/main.py
git commit -m "T4: lifespan starts start_http_server sidecar on METRICS_PORT; remove routes/metrics

- main.py:19: drop metrics from routes import list
- main.py lifespan 开头: optional multiproc dir setup + MultiProcessCollector
- main.py lifespan: start_http_server(METRICS_PORT default 9090, METRICS_HOST default 127.0.0.1)
- app.state.metrics_httpd stores WSGIServer handle for shutdown
- lifespan yield 后: httpd.shutdown() then httpd.server_close() to release socket
- create_app(): drop app.include_router(metrics.router)
- env vars PROMETHEUS_MULTIPROC_DIR / METRICS_PORT / METRICS_HOST 配置驱动

Tests 1+2 should now pass; test 3 (shutdown release) verified in T5."
```

---

### Task 5: 验证 test 3(端口 release)+ 加 retry 容忍

**Files:**
- Modify: `rag/tests/integration/test_metrics_exporter.py`(仅 test 3 的连接重试逻辑)

- [ ] **Step 1: 跑 test 3 单独验证**

```bash
cd rag && python -m pytest tests/integration/test_metrics_exporter.py::test_exporter_stops_on_lifespan_exit -v --tb=long 2>&1 | tail -30
```

观察:可能 PASS,可能 fail in `httpx.ConnectError` 等待(因为 socket 状态 TIME_WAIT 内会 accept 但 listen 已断)。

- [ ] **Step 2: 若 PASS,跳到 Step 4**

若 FAIL,继续 Step 3。

- [ ] **Step 3: 修 test 3 加 bind-retry 容忍 TIME_WAIT**

`test_exporter_stops_on_lifespan_exit`(in `test_metrics_exporter.py`)改为:

```python
def test_exporter_stops_on_lifespan_exit(sidecar_env):
    """After lifespan teardown, the port is no longer reachable."""
    app = create_app()
    with app.router.lifespan_context(app):
        url = f"http://127.0.0.1:{sidecar_env['port']}/metrics"
        resp = httpx.get(url, timeout=2.0)
        assert resp.status_code == 200

    # Lifespan exited. server_close() should release immediately, but kernel
    # may briefly retain state; retry connect until refused.
    import time
    refused = False
    deadline = time.time() + 3.0
    while time.time() < deadline:
        try:
            httpx.get(url, timeout=0.5)
            time.sleep(0.05)  # still up; wait a tick
        except (httpx.ConnectError, ConnectionRefusedError, OSError):
            refused = True
            break
    assert refused, f"exporter still reachable on {url} after lifespan exit"
```

(用 retry loop 容忍 50ms 内剩余的 socket 状态。)

- [ ] **Step 4: 跑全部 3 测试 → 期望 3 PASS**

```bash
cd rag && python -m pytest tests/integration/test_metrics_exporter.py -v --tb=short 2>&1 | tail -10
```

Expected: `3 passed`。

- [ ] **Step 5: Commit**

```bash
cd /home/pangzy/code_project/EKRS
git add rag/tests/integration/test_metrics_exporter.py
git commit -m "T5: test_exporter_stops_on_lifespan_exit retries socket release

server_close() releases the listening socket but the kernel may briefly
retain state. Loop with 0.5s timeout up to 3s waits for ConnectError.
Avoids flakiness from kernel-side TIME_WAIT/CLOSE_WAIT windows."
```

---

### Task 6: .env.example 更新(添加 + 删除)

**Files:**
- Modify: `.env.example` (repo root `/home/pangzy/code_project/EKRS/.env.example`)

- [ ] **Step 1: 读现有 .env.example line 45-50 区段**

```bash
cd /home/pangzy/code_project/EKRS
sed -n '45,50p' .env.example
```

确认当前结构。原 line 48 `# METRICS_TOKEN=changeme  # 可选: /metrics 鉴权 (留空 = 不鉴权)`。

- [ ] **Step 2: 添加 Phase 5.5 D env vars**

在文件末尾追加:

```bash
# Phase 5.5 D: Metrics sidecar exporter (与 RAG 主端口 8000 分离)
# PROMETHEUS_MULTIPROC_DIR: 多 worker 模式必填;单 worker 留空即可。
# 必须在 uvicorn fork 子进程前设置(部署层面的 env)。
# PROMETHEUS_MULTIPROC_DIR=/tmp/prom_multiproc
METRICS_HOST=127.0.0.1
METRICS_PORT=9090
```

(6 行:3 注释 + 1 注释 env + 2 实际 default。)

- [ ] **Step 3: 删除原 line 48 `METRICS_TOKEN` 注释**

找到该行(grep 验证):

```bash
grep -n "METRICS_TOKEN" .env.example
```

如存在,用 Edit 工具删除该行。删除后:

```bash
grep "METRICS_TOKEN" .env.example
# 期望: 无输出
```

- [ ] **Step 4: Commit**

```bash
cd /home/pangzy/code_project/EKRS
git add .env.example
git commit -m "T6: .env.example — add METRICS_HOST/PORT/PROMETHEUS_MULTIPROC_DIR, remove METRICS_TOKEN

- +METRICS_HOST=127.0.0.1 +METRICS_PORT=9090 +PROMETHEUS_MULTIPROC_DIR guide
- -line 48 METRICS_TOKEN comment (主端口已无 /metrics,鉴权失效)
- 部署层(env vars at docker-compose / k8s / shell)必须在 uvicorn 启动前设 multiproc"
```

---

### Task 7: deployment/docker-compose.yml 加 prometheus 服务

**Files:**
- Modify: `deployment/docker-compose.yml` (新增 prometheus service 块)

- [ ] **Step 1: 读现有 docker-compose.yml**

```bash
cd /home/pangzy/code_project/EKRS
cat deployment/docker-compose.yml
```

确认结构(version 3.8, 3 services: qdrant / redis / rag; volumes 区块在末尾)。

- [ ] **Step 2: 在 rag service 之后、volumes 区块之前插入 prometheus service**

修改 `deployment/docker-compose.yml`,在 rag service 后插入:

```yaml
  prometheus:
    image: prom/prometheus:latest
    ports:
      - "19090:9090"          # host:19090 -> container:9090 (UI / sanity probe)
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.retention.time=7d'
    depends_on:
      rag:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 5s
      retries: 3
```

约 17 行。在 `volumes:` 区块前(`- ./prometheus.yml:/etc/...`)新增。

- [ ] **Step 3: 验证 docker-compose 语法**

```bash
cd /home/pangzy/code_project/EKRS
docker compose -f deployment/docker-compose.yml config --quiet 2>&1 | tail -5 || \
docker-compose -f deployment/docker-compose.yml config --quiet 2>&1 | tail -5
```

Expected: 无错误输出。如果 docker 不在 PATH,跳过此 step 验证,直接 commit。

- [ ] **Step 4: Commit**

```bash
cd /home/pangzy/code_project/EKRS
git add deployment/docker-compose.yml
git commit -m "T7: docker-compose — add prometheus service scraping rag:9090

- prometheus host port 19090 -> container 9090 (UI only, 不撞 rag sidecar)
- volumes ./prometheus.yml 挂入配置文件
- depends_on rag service_healthy (Phase 4 healthcheck 已有)
- 7 day retention; wget -q spider healthcheck
- sidecar (:9090) 不从 host 暴露,只有 prometheus 容器可达"
```

---

### Task 8: deployment/prometheus.yml scrape 配置

**Files:**
- Create: `deployment/prometheus.yml`

- [ ] **Step 1: 创建文件**

```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: ekrs-rag
    static_configs:
      - targets: ['rag:9090']    # 容器内互访走 docker network DNS
    metrics_path: /metrics
```

12 行。保存为 `deployment/prometheus.yml`。

- [ ] **Step 2: 验证 YAML 语法**

```bash
cd /home/pangzy/code_project/EKRS
python -c "import yaml; yaml.safe_load(open('deployment/prometheus.yml'))" && echo "OK"
```

Expected: `OK`。

- [ ] **Step 3: Commit**

```bash
cd /home/pangzy/code_project/EKRS
git add deployment/prometheus.yml
git commit -m "T8: prometheus.yml — scrape ekrs-rag target on rag:9090

- scrape_interval 15s (default; tunable)
- target 'rag:9090' (docker compose 默认网络内服务名解析)
- metrics_path /metrics (default; included for clarity)
- 无 PromQL alerting rules (Phase 5.5 deferred)"
```

---

### Task 9: Final 验收 + 全套件测试 + tag

**Files:**
- 不改代码,只跑验收。

- [ ] **Step 1: 主端口 `/metrics` 必须 404**

```bash
cd /home/pangzy/code_project/EKRS
cd rag && python -c "
from fastapi.testclient import TestClient
from ekrs_rag.main import create_app
app = create_app()
with TestClient(app) as c:
    r = c.get('/metrics')
    print(f'status: {r.status_code}')
    assert r.status_code == 404, f'expected 404, got {r.status_code}'
print('OK')
"
```

Expected: `status: 404` 然后 `OK`。

- [ ] **Step 2: Sidecar 在 :9090 暴露**

```bash
cd /home/pangzy/code_project/EKRS
cd rag && METRICS_PORT=19091 python -c "
from ekrs_rag.main import create_app
import httpx
app = create_app()
with app.router.lifespan_context(app):
    r = httpx.get('http://127.0.0.1:19091/metrics', timeout=2.0)
    print(f'status: {r.status_code}')
    print(f'first line: {r.text.split(chr(10))[0]}')
    assert r.status_code == 200
    assert '# HELP' in r.text
print('OK')
"
```

Expected: `status: 200` + `first line: # HELP ...` + `OK`。

- [ ] **Step 3: 跑全套件**

```bash
cd rag && python -m pytest tests/ -q --tb=short 2>&1 | tail -5
```

Expected: `312 passed, 1 skipped, 0 failed`(基线 318 减去删掉的 3 个 test_metrics_endpoint 测试;新 3 个 test_metrics_exporter 测试补上;减 6 = 加 3 = 基线减 3 = 315;当前 baseline 318 = 318,略减)。

实际:基线 318 - 3 (test_metrics_endpoint 删除) + 3 (test_metrics_exporter 新增) = 318 不变(测试数守恒)。1 skipped 不变。

- [ ] **Step 4: 跑覆盖率(对照 78% baseline)**

```bash
cd rag && python -m pytest tests/ -q --cov=ekrs_rag --cov-report=term 2>&1 | tail -3
```

Expected: 整体 ≥ 78%(没有新增未测模块,改动在 lifespan + tests/conftest + deployment,应与 baseline 持平或略升)。

- [ ] **Step 5: grep 验证删除项**

```bash
cd /home/pangzy/code_project/EKRS
grep -rn "routes.metrics\|api/routes/metrics" rag/ --include="*.py" && echo "FOUND" || echo "OK: no refs"
grep "METRICS_TOKEN" .env.example && echo "FOUND" || echo "OK: METRICS_TOKEN removed"
grep -rn "from .api.routes import.*metrics" rag/ --include="*.py" && echo "FOUND" || echo "OK: no metrics import"
grep "include_router(metrics.router)" rag/ -r --include="*.py" && echo "FOUND" || echo "OK: no metrics router"
```

Expected: 4 个 `OK`。

- [ ] **Step 6: 更新 ledger + tag**

```bash
cd /home/pangzy/code_project/EKRS
cat > .superpowers/sdd/PHASE-5.5-D-COMPLETE.md <<'EOF'
# Phase 5.5 D: /metrics 主端口下线 → Sidecar Exporter — DONE

**Date:** 2026-07-13
**Range:** <COMMIT_BASE>..<COMMIT_HEAD>
**Commits:** 9 (T1-T9)
**Tests:** 318 passed, 1 skipped, 0 failed

All spec §验收 checks pass:
- 主端口 /metrics 404
- Sidecar :9090 暴露 Prometheus text
- 3 test_metrics_exporter 测试 pass
- 文件清理: routes/metrics.py + test_metrics_endpoint.py
- .env.example: +METRICS_HOST/PORT/-METRICS_TOKEN
- 主端口 import metrics 引用已删
- coverage ≥ 78%

Out of scope (unchanged):
- Phase 5.5 A/B/C/E: 各自 spec/plan
- /healthz 暴露 exporter alive: Phase 6 follow-up
- Alertmanager / PromQL 报警规则
- Iron Rules / audit schema / Phase 5 replay 行为
EOF

git add .superpowers/sdd/PHASE-5.5-D-COMPLETE.md
git commit -m "T9: phase 5.5 D complete ledger + tag phase5.5-d-metrics-exporter"

git tag -a phase5.5-d-metrics-exporter -m "Phase 5.5 D: /metrics 主端口下线,sidecar exporter on :9090 + prometheus scrape

Range: T1-T9 (9 commits)
Removed: routes/metrics.py + test_metrics_endpoint.py
Added: test_metrics_exporter.py (3 tests + free_port helper)
Wired: main.py lifespan start_http_server (multiproc optional)
Infra: docker-compose.yml prometheus service + prometheus.yml scrape

Tests: 318 passed, 1 skipped, 0 failed
Coverage: ≥ 78% aggregate

Design: docs/superpowers/specs/2026-07-13-phase5.5-metrics-exporter-design.md
Plan:   docs/superpowers/plans/2026-07-13-phase5.5-metrics-exporter.md"

git tag -n5 phase5.5-d-metrics-exporter | head -8
```

- [ ] **Step 7: 最终 commit ledger**

```bash
git add .superpowers/sdd/PHASE-5.5-D-COMPLETE.md
git commit --allow-empty -m "T9: tag ledger confirmed"
```

(若 Step 6 已 commit ledger 此步仅是 sanity。)

---

## Self-Review (writing-plans 清单)

**Spec 覆盖:**
- D1 删 routes/metrics.py + main.py 不 include_router → T1 + T4 覆盖 ✓
- D2 lifespan start_http_server → T4 覆盖 ✓
- D3 PROMETHEUS_MULTIPROC_DIR 可选、env 可配 → T4 + T6 覆盖 ✓
- D4 shutdown + server_close → T4 覆盖(实现)+ T5(test 容忍 TIME_WAIT) ✓
- D5 重写 test + free_port helper → T3 + T5 覆盖 ✓
- .env.example line 48 METRICS_TOKEN 删除 → T6 覆盖 ✓
- docker-compose prometheus service → T7 覆盖 ✓
- prometheus.yml scrape → T8 覆盖 ✓
- conftest multiproc collector 清理 → T2 覆盖 ✓

**Acceptance grep + curl + 测试:** T9 覆盖 ✓

**占位符扫描:** 全文 grep "TBD/TODO/FIXME/待补"= 0 命中。

**类型/签名一致性:**
- `free_port() -> int` (T3) ↔ 调用方 `port = free_port()` + `monkeypatch.setenv("METRICS_PORT", str(port))` (T3 fixture) ✓
- `app.state.metrics_httpd` (T4 lifespan) ↔ `getattr(app.state, "metrics_httpd", None)` (T4 shutdown) ✓
- `MULTIPROCESS_COLLECTOR` collectors 前缀 `prometheus_multiproc_*` (T2 conftest) ↔ `MultiProcessCollector(REGISTRY)` 注册 (T4 lifespan) ✓
- `routes/metrics.py` 删除 (T1) ↔ main.py 不 import (T4) ↔ main.py 不 include_router (T4) ✓
- test_metrics_endpoint.py 删除 (T1) ↔ test_metrics_exporter.py 创建 (T3) ✓

无 plan-internal 一致性问题。

---

## 执行 Handoff(Phase 5.5 D plan 已落档,不立即执行)

Plan 落档: `docs/superpowers/plans/2026-07-13-phase5.5-metrics-exporter.md`
Spec 同步: `docs/superpowers/specs/2026-07-13-phase5.5-metrics-exporter-design.md`

按 user 选:"生成 plan 但不立即实施"(与 E 模式一致)。执行触发词:用户重启时给"继续"或具体 task 编号。

## 尚未解决 (per CLAUDE.md 规则)

- **A/B/C/E 子项目**:未 plan,与 D 独立。
- **/healthz 暴露 exporter alive**:Phase 6 follow-up task(spec 在"不在范围"段已记)。
- **docker-compose 启动验证**:T7 Step 3 可选(若 docker 不在 PATH,跳过验证),生产前 `make dev-up` 端到端 run 一次。
- **spec §错误处理 提到 `_isolate_prometheus_registry` 已处理 counter 重名**;T2 加了对 `prometheus_multiproc_*` 的清理,与原处理对称。如未来有新 prefix 类 collector,conftest 模式可能需扩展。
