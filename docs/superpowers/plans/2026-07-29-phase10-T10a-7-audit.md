# Phase 10 T10a-7 — 审计事件 fts_synced + fts_searched + phase10 tag closure

## 父计划

[`2026-07-28-phase10-broad-spectrum-retrieval.md`](2026-07-28-phase10-broad-spectrum-retrieval.md) §T10a-7 行 (line 32).

## 范围

T10a-7 是 broad-spectrum retrieval 最后一块: 把 Phase 10 累积的 FTS 写入 + 检索事件**完整接通到审计**, 并以 `phase10` 标签锁闭合.

1. **`fts_synced` schema 注册** — `event count 20→21`. 写入点已有 (T10a-2 pipeline.ingest Step 5.6 emit), 现在补 schema. required fields: `doc_hash`, `version`, `chunks_written`. **parent §[M2] 关闭**: 不进入 `IngestionOutcome` enum (中间步骤, 非最终态).
2. **`fts_searched` schema 注册** — `event count 21→22`. required fields: `vector_hits`, `fts_hits`, `both_hits` (来自 T10a-3 `FusionStats`). 写入点: retriever `retrieve()` 内 RRF 调用后 emit, 当 `self._fts is not None`.
3. **`EKRSRetriever` 加 `audit_writer` kwarg** — 镜像 Phase 5.5 E DI 模式. `audit_writer=None` (默认) = 不 emit (Phase 9 byte-level baseline); main.py lifespan 注入 `_audit_writer`. **不**用 module-global `get_writer()` (避免 Phase 5.5 E 迁移回滚).
4. **lifespan 注入 audit_writer 到 retriever** — `_retriever = EKRSRetriever(qdrant=..., fts=..., audit_writer=_audit_writer)`.
5. **`phase10` annotated tag** — 锁 T10a-7 final commit (含 schema 注册 + retriever emit + 全部 unit/integration test pass). **force-move** (per parent §111 "annotated tag force-move 到 phase 整体闭合 commit").
6. **CHANGELOG.md `[phase10]` release section + 完整 timeline** — 0.0.5 → 0.1.0 minor bump (Phase 10 是新能力, minor OK per Keep-a-Changelog).

**T10a-7 边界**: 审计事件 + tag 闭合. **不做**:
- `IngestionOutcome` enum 加字段 (parent §204 明确关闭).
- 跨引擎 rerank (T10c, 不触发).
- schema 改动 `ConstraintQueryResponse` 或 `RetrievalResult`.

## 设计

### `fts_synced` schema

```python
# main.py _EVENT_SCHEMAS (line 83-110)
"fts_synced": {"doc_hash", "version", "chunks_written"},
```

写入点 (T10a-2 已有):

```python
# pipeline.py:207
self._audit_writer.write(
    "fts_synced",
    doc_hash=doc_hash, version=version, chunks_written=count,
)
```

### `fts_searched` schema

```python
"fts_searched": {"vector_hits", "fts_hits", "both_hits"},
```

写入点 (T10a-7 NEW):

```python
# retriever.py retrieve() — after RRF fusion, before return
if self._audit_writer is not None and fusion_stats is not None:
    self._audit_writer.write(
        "fts_searched",
        vector_hits=fusion_stats.vector_hits,
        fts_hits=fusion_stats.fts_hits,
        both_hits=fusion_stats.both_hits,
    )
```

### Retriever audit_writer DI

```python
class EKRSRetriever:
    def __init__(
        self,
        qdrant: QdrantManager,
        fts: Optional["object"] = None,
        audit_writer: Optional["AuditWriter"] = None,  # T10a-7 NEW
    ) -> None:
        self._qdrant = qdrant
        self._fts = fts
        self._audit_writer = audit_writer  # T10a-7
```

**Phase 9 byte-level baseline**: `audit_writer=None` → 无 emit, 与 Phase 9 既有 retriever 完全一致.

### Lifespan wiring

```python
# main.py lifespan
_retriever = EKRSRetriever(
    qdrant=_qdrant_manager,
    fts=_fts_manager,
    audit_writer=_audit_writer,  # T10a-7
)
```

### Iron Rules 合规

| Rule | 影响 |
|---|---|
| R1 | 审计事件不参与 hint 提取 |
| R2 | retriever 不变 (audit_writer 是新 DI, 不改 RRF 逻辑) |
| R3 | 审计事件在三闸门 *外* 触发 (T10a-2 同步写 FTS, T10a-7 检索 emit), 不影响 recall/extract/solve |
| R4 | scope_priority 仍 AFTER RRF; emit 在 RRF 后, 不影响排序 |
| R5 | 不引入 KG |
| R7 | scope_filter 不变 |
| R8 | `status != 'illegal'` 不变 |

## TDD 任务 (Tg.1 / Tg.2 / Tg.3 / Tg.4 / Tg.5)

| # | 任务 | 工作量 | 验收 |
|---|---|---|---|
| **Tg.1** | RED: fts_searched emit test + fts_synced schema test + retriever audit_writer=None byte-level test | 0.25 天 | ≥5 个 fail test |
| **Tg.2** | GREEN: fts_searched emit + audit_writer DI + schema registration | 0.25 天 | 5 测试 pass |
| **Tg.3** | IMPROVE: 边界 (audit_writer=None 不抛 + fts=None 不 emit + FusionStats 0/0/0 emit) | 0.25 天 | 3 边界测试 pass |
| **Tg.4** | 文档 + CHANGELOG release section + memory + tag | 0.25 天 | CHANGELOG + handbook + memory + phase10 annotated tag |
| **Tg.5** | FF push master + 全部阶段 sanity check | 0.25 天 | push OK + remote tags 可见 |

### Tg.1 测试枚举

`tests/unit/test_audit_event_coverage_t10a7.py` ≥5 测试:

1. `test_fts_searched_emit_with_fusion_stats` — retriever 配 audit_writer mock + fts 配置 → 1 emit with vector_hits/fts_hits/both_hits
2. `test_fts_searched_schema_registration` — AuditLogger.register_event_schema("fts_searched", {...}) + log_event 验证 validate
3. `test_retrieve_with_audit_writer_none_does_not_emit` — retriever audit_writer=None → no emit (Phase 9 byte-level)
4. `test_fts_synced_schema_required_fields` — schema register + log_event 验证 required doc_hash/version/chunks_written
5. `test_retrieve_fusion_stats_with_zero_fts_hits_still_emits` — fts_hits=0 时 emit 仍然发生 (FusionStats(vector=N, fts=0, both=0))

### Tg.3 IMPROVE 边界测试

- `test_retrieve_no_emit_when_fts_disabled` — fts=None → 不 emit fts_searched (Phase 9 baseline)
- `test_retrieve_emit_does_not_fail_when_audit_writer_raises` — audit_writer.write 抛异常 → retriever 不传异常 (best-effort, 审计永远不阻塞检索)
- `test_fts_searched_event_count_22` — main.py _EVENT_SCHEMAS 包含 fts_synced + fts_searched, 总数 22 (从 20 baseline 增 2)

### Tg.4 文档

- CHANGELOG.md 加 `[phase10] - 2026-07-29` release section (Keep-a-Changelog format: Added/Changed/Fixed 全部 T10a-1..7)
- ekrs-handbook.md §6 timeline 加 T10a-7 行
- CLAUDE.md Current State 加 T10a-7 行 + phase10 tag 行
- memory `phase10-closure.md`
- MEMORY.md 加 pointer

### Tg.5 Tag closure

- `git tag -a phase10 -m "..." <final-commit-sha>`
- `git push origin phase10 --force` (force-move per parent §111)
- `git push origin master` (FF)
- 验证 `git ls-remote --tags origin | grep phase10`

## 标签策略

| Tag | 锁定 commit | 状态 |
|---|---|---|
| `phase10.1` | `1c44eee` (T10b-1) | DO NOT MOVE |
| **`phase10`** | **T10a-7 final commit** | **T10a-7 force-move 到这里** |

父计划 §111: annotated tag force-move 到 phase 整体闭合 commit. **本任务开新 tag**.

## 验证闸门

- [ ] `tests/unit/test_audit_event_coverage_t10a7.py` ≥5 测试 pass
- [ ] Phase 7 T1 (qdrant_write_failed 集成测试) 不退化
- [ ] 完整 suite 816+ 测试 0 退化
- [ ] mypy 干净
- [ ] CHANGELOG.md `[phase10]` release section 已写
- [ ] ekrs-handbook.md §6 timeline 加 T10a-7 行
- [ ] memory `phase10-closure.md` 已写
- [ ] `phase10` annotated tag 锁在 T10a-7 final commit
- [ ] FF push master 成功
- [ ] `git ls-remote --tags origin | grep phase10` 看到新 tag

## 风险

| 风险 | 缓解 |
|---|---|
| audit emit 抛异常阻断 retriever | `try/except` 包 emit; 失败 log warning, 不传异常 (parent §204 "审计永远不阻塞业务") |
| fts_searched schema 字段不足 | FusionStats 三字段已就位 (T10a-3), 直接 consume |
| fts_synced 写入点 schema 不匹配 (pipeline.py 现有 emit 已用 doc_hash/version/chunks_written) | Tg.1 #4 验证 required field set 完全匹配 |
| `phase10` tag force-move 风险 (覆盖既有 tag) | parent §111 明示 "annotated tag force-move 是 closure 行为"; 旧 `phase10` 是 T10a-2 临时 commit, 已经被 T10a-7 commit 取代; force-move OK |
| IngestionOutcome enum 被误改 (parent §204 明示禁止) | Tg.3 IMPROVE 测试验证 enum 不增字段 |

## 开放问题 (实施前关闭)

1. ~~**fts_synced 是否补 schema**~~ — **关闭**: 补. event count 20→21. parent §32 明确要求.
2. ~~**fts_searched schema 字段集**~~ — **关闭**: `{vector_hits, fts_hits, both_hits}` 来自 T10a-3 `FusionStats`. RRF 融合已返回 FusionStats; retriever 直接取三字段.
3. ~~**retriever audit_writer DI vs module-global get_writer()**~~ — **关闭**: DI (kwarg). Phase 5.5 E 已迁 module globals → FastAPI Depends; T10a-7 沿用 DI 模式, 不回退. 兼容: `audit_writer=None` 默认无 emit (Phase 9 baseline).
4. ~~**fts=None 时是否 emit**~~ — **关闭**: 不 emit. 退化路径 = Phase 9 byte-level, 与 `fusion_stats=None` 同步 (consumer 端 `is None` 判定).
5. ~~**CHANGELOG release section 格式**~~ — **关闭**: Keep-a-Changelog standard: `[phase10] - 2026-07-29` + Added (T10a-1..7 全部) + Changed + Fixed. version bump 0.0.5 → 0.1.0 (Phase 10 是新能力, minor OK).
6. ~~**memory record 文件名**~~ — **关闭**: `phase10-closure.md` (强调 closure, 不只 T10a-7).

**已无未关闭问题. 可开始 Tg.1 RED.**

## GSTACK REVIEW REPORT

**Run:** 1 (self-review pre-implementation) · **Status:** clean (1 patch recommended, applied below)
**Date:** 2026-07-29
**Reviewer:** claude (eng-review pass)

### Findings — 4 项 (1 MEDIUM + 3 INFO)

| # | Severity | Conf | Finding | Resolution |
|---|---|---|---|---|
| [M1] | MEDIUM | 8/10 | `fts_searched` 写入点若 audit_writer 抛异常, 可能阻断 retriever 业务路径 | ✅ Tg.3 IMPROVE 加 `test_retrieve_emit_does_not_fail_when_audit_writer_raises` + retriever emit 用 try/except 包, log warning |
| [I1] | INFO | 6/10 | event count 20→22 (跨 T10a-2 + T10a-7), 需在 main.py _EVENT_SCHEMAS 同时注册 2 个 | ✅ 计划 §验证闸门 加 `test_fts_searched_event_count_22` 锁定总数 |
| [I2] | INFO | 5/10 | `phase10` tag force-move 是 closure 行为, 旧 commit 被覆盖; 需明确告知 user | ✅ Tg.5 步骤 + memory `phase10-closure.md` 记录 force-move 行为 + 旧 commit SHA |
| [I3] | INFO | 4/10 | CHANGELOG release section 体量大 (T10a-1..7 8 个段落) | ✅ 直接搬 `[Unreleased] ## Added` 已写段落, 不重写; 顺序按 T10a-1..7 |

### Run 1 Verdict

**QUALITY: 8.0/10**. 1 MEDIUM (M1 audit exception isolation) 已 patch + 测试覆盖; 3 INFO mitigation 文档化. **Ready to implement** — proceed Tg.1 RED.