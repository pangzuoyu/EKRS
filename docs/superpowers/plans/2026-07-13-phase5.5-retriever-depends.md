# Phase 5.5 E: Routes 全局 → FastAPI Depends — Implementation Plan

> **Augmentations from gstack-plan-eng-review (2026-07-13):**
> See [§ Augmentations](#augmentations-gstack-plan-eng-review) at the end for the 6 findings from the engineering review and how each Task was patched. **P1 fixes are inline in the affected Steps below.**

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 替换 5 个 module-level `_X` 全局为 FastAPI `Depends(get_x)`,所有读取走 `app.state.x`,测试用 `app.dependency_overrides`。

**Architecture:** 5 个新依赖函数(2 strict + 1 optional + 2 strict)。Lifespan 写 `app.state.x`。Strict 依赖在 `app.state.x is None` 时抛 503。测试用 `app.dependency_overrides[get_x] = lambda: mock`。

**Tech Stack:** FastAPI Depends、app.state、app.dependency_overrides。无新依赖。

**Spec:** `docs/superpowers/specs/2026-07-13-phase5.5-retriever-depends-design.md`

---

## Global Constraints

- 严格按 spec §设计决策 D1-D5:`get_audit_index` 是唯一 Optional(返回 None);其他 4 个 strict(抛 503)
- 不改 Iron Rules R1-R7
- 不改 15 个 audit event 名称或 schema
- 不改 Phase 5 replay/`deterministic_match` 业务行为
- 不动 `_isolate_prometheus_registry` conftest(独立 Prom collector 隔离)
- TDD:每 task 先写 failing test → 实现 → pass → commit
- 每 task 单 commit(message 包含 task 编号前缀,如 `T1:`)
- 工作目录: `cd rag && ...`

---

## File Inventory

| 类型 | 路径 |
|---|---|
| Modify | `rag/ekrs_rag/api/routes/constraints.py` (-2 globals + -2 setters + +2 deps + 1 route sig) |
| Modify | `rag/ekrs_rag/api/routes/ingestion.py` (-3 globals + -3 setters + +3 deps + 3 route sigs) |
| Modify | `rag/ekrs_rag/main.py` (-5 set_X calls + +5 app.state.X writes) |
| Create | `rag/tests/unit/api/test_route_dependencies.py` (5 单测,验证契约) |
| Modify | `rag/tests/integration/test_query_replay.py` (删 fixture + 改 2 set_X 为 overrides) |
| Modify | `rag/tests/integration/test_ingestion_replay.py` (改 5 set_X 为 overrides) |
| Modify | `rag/tests/integration/test_ingestion.py` (改 3 set_X 为 overrides) — eng-review gap |
| Modify | `rag/tests/integration/test_ingestion_phase4.py` (改 3 set_X 为 overrides + teardown) |

净变化 ≈ +25 / -15 行。无新文件依赖。

---

### Task 1: 5 个 Dep 函数 + 5 个单测契约验证

**Files:**
- Modify: `rag/ekrs_rag/api/routes/constraints.py:1-50`(顶部 imports 后)
- Modify: `rag/ekrs_rag/api/routes/ingestion.py:1-55`(顶部 imports 后)
- Create: `rag/tests/unit/api/test_route_dependencies.py`

**Interfaces:**
- Consumes: 标准 FastAPI Request;`HTTPException`;`EKRSRetriever`、`AuditIndex`、`IngestionPipeline`、`RedisLock`、`TaskRepo` types
- Produces: 5 个依赖函数:`get_retriever(request)` strict、`get_audit_index(request)` Optional、`get_pipeline(request)` strict、`get_redis_lock(request)` strict、`get_task_repo(request)` strict。None of these are wired to routes yet — 仅本任务内被新单测使用。

- [ ] **Step 1: 写新单测文件(failing)**

```python
# rag/tests/unit/api/test_route_dependencies.py
"""Contract tests for route dependency helpers added in Phase 5.5 E.

Each dependency is verified in isolation (no FastAPI app boot) by directly
invoking it with a mock request whose app.state is a real State() instance
configured to None. (P1 fix from gstack-plan-eng-review: MagicMock auto-
creates `state.X` as a truthy attribute, defeating the 503 check.)
"""
import pytest
from starlette.datastructures import State
from fastapi import HTTPException

from ekrs_rag.api.routes.constraints import get_retriever, get_audit_index
from ekrs_rag.api.routes.ingestion import (
    get_pipeline, get_redis_lock, get_task_repo,
)


def _mock_request(state_overrides: dict | None = None):
    """Build a mock Request whose app.state is a real State() (no auto-attr)."""
    from unittest.mock import MagicMock
    req = MagicMock()
    req.app.state = State()  # real State; missing attrs raise AttributeError
    if state_overrides:
        for k, v in state_overrides.items():
            setattr(req.app.state, k, v)
    return req


def test_get_retriever_raises_503_when_state_unset():
    req = _mock_request()
    with pytest.raises(HTTPException) as exc:
        get_retriever(req)
    assert exc.value.status_code == 503
    assert "retriever" in exc.value.detail


def test_get_audit_index_returns_none_optional():
    req = _mock_request()
    # Optional dep returns None, does NOT raise
    assert get_audit_index(req) is None


def test_get_pipeline_raises_503_when_state_unset():
    req = _mock_request()
    with pytest.raises(HTTPException) as exc:
        get_pipeline(req)
    assert exc.value.status_code == 503
    assert "pipeline" in exc.value.detail


def test_get_redis_lock_raises_503_when_state_unset():
    req = _mock_request()
    with pytest.raises(HTTPException) as exc:
        get_redis_lock(req)
    assert exc.value.status_code == 503
    assert "redis" in exc.value.detail.lower()


def test_get_task_repo_raises_503_when_state_unset():
    req = _mock_request()
    with pytest.raises(HTTPException) as exc:
        get_task_repo(req)
    assert exc.value.status_code == 503
    assert "task repo" in exc.value.detail.lower()
```

- [ ] **Step 2: 运行测试 → 期望 FAIL (函数不存在)**

```bash
cd rag && python -m pytest tests/unit/api/test_route_dependencies.py -v --tb=short
```

Expected: `ImportError` 或 `AttributeError: module ... has no attribute 'get_retriever'`.

- [ ] **Step 3: 加 5 个依赖函数**

修改 `rag/ekrs_rag/api/routes/constraints.py` 第 1-50 行(顶部 imports 之后,路由定义之前;现有模块级 globals + setters 暂保留以便 Route 服务在 T3 之前不破):

```python
# 在文件中加入(路由定义前,全局 globals 旁边或其后):

from fastapi import HTTPException  # 加入已有 imports


def get_retriever(request: Request) -> EKRSRetriever:
    """Strict dep: read retriever from app.state. 503 if uninitialized."""
    r = getattr(request.app.state, "retriever", None)
    if r is None:
        raise HTTPException(status_code=503, detail="retriever not initialized")
    return r


def get_audit_index(request: Request) -> AuditIndex | None:
    """Optional dep: returns AuditIndex or None. Replay branch checks None."""
    return getattr(request.app.state, "audit_index", None)
```

修改 `rag/ekrs_rag/api/routes/ingestion.py` 顶部:

```python
from fastapi import HTTPException  # 加入

# 在路由定义之前加入:


def get_pipeline(request: Request) -> IngestionPipeline:
    p = getattr(request.app.state, "pipeline", None)
    if p is None:
        raise HTTPException(status_code=503, detail="ingestion pipeline not initialized")
    return p


def get_redis_lock(request: Request) -> RedisLock:
    lock = getattr(request.app.state, "redis_lock", None)
    if lock is None:
        raise HTTPException(status_code=503, detail="redis lock not initialized")
    return lock


def get_task_repo(request: Request) -> TaskRepo:
    repo = getattr(request.app.state, "task_repo", None)
    if repo is None:
        raise HTTPException(status_code=503, detail="task repo not initialized")
    return repo
```

- [ ] **Step 4: 运行测试 → 期望 PASS**

```bash
cd rag && python -m pytest tests/unit/api/test_route_dependencies.py -v --tb=short
```

Expected: 5 passed. (注:`_mock_request()` 不显式设 `retriever` 等,所以 `getattr(..., None)` 触发,strict 抛 503,optional 返回 None.)

- [ ] **Step 5: Commit**

```bash
cd /home/pangzy/code_project/EKRS && git add rag/ekrs_rag/api/routes/constraints.py rag/ekrs_rag/api/routes/ingestion.py rag/tests/unit/api/test_route_dependencies.py
git commit -m "T1: add 5 route dependencies (get_retriever/audit_index/pipeline/lock/repo) + contract tests

- get_retriever strict: 503 if app.state.retriever is None
- get_audit_index optional: returns None (replay branch checks)
- get_pipeline/get_redis_lock/get_task_repo strict: 503 on None
- 5 unit tests verify dep functions in isolation (mocked request)
- Routes still use module-level globals; conversion deferred to T2/T3"
```

---

### Task 2: Constraints.py route 改用 Depends + 删除 globals/setters

**Files:**
- Modify: `rag/ekrs_rag/api/routes/constraints.py`(路由签名 + 删除 `_retriever`/`_audit_index` + 删 `set_retriever`/`set_audit_index` + 删 fallback `getattr(...)` + replay 分支用 audit_index 形参)

**Interfaces:**
- Consumes: Task 1 提供的 `get_retriever`、`get_audit_index`(本任务起路由开始依赖它们)
- Produces: `query_constraints(...)` 新签名:`(query: ConstraintQuery, retriever: EKRSRetriever = Depends(get_retriever), audit_index: AuditIndex | None = Depends(get_audit_index), _auth: None = Depends(require_parser_token))`。模块中 `_retriever`/`_audit_index`/`set_retriever`/`set_audit_index` 4 个标识符全部消失。

- [ ] **Step 1: 写 failing 集成测试验证新签名(可选;已有 test_query_replay.py 改写迁移,但此处先扩一个 sanity test)**

在 `tests/integration/test_query_replay.py` 加(稍后 T4 会将其与他测试一起迁移,现在先不改整个文件):

```python
def test_constraints_route_uses_dependency_overrides():
    """Constraints route gets retriever via Depends; overrides take precedence."""
    from ekrs_rag.api.routes.constraints import router, get_retriever
    app = FastAPI()
    app.include_router(router)

    sentinel = object()
    captured = {}

    class MockRetriever:
        def retrieve(self, query, top_k, active_scope=None):
            captured["query"] = query
            return RetrievalResult(chunks=[], scores={})

    app.dependency_overrides[get_retriever] = lambda: MockRetriever()
    client = TestClient(app)
    resp = client.post("/v1/constraints", json={"query": "x"})
    assert resp.status_code == 404  # Gate 1 — empty chunks
    assert captured["query"] == "x"
```

Expected after T2 implementation: PASS(proves dep wired).

- [ ] **Step 2: 跑测试 → FAIL(路由仍用 _retriever 全局)**

跳到 Step 3 之前,先确认当前的路由行为 — 跑测试应 FAIL,因为路由读的是 module global(默认 None),即使 override 注入 mock 也不走 override。

```bash
cd rag && python -m pytest tests/integration/test_query_replay.py -v --tb=short -k "test_constraints_route_uses_dependency_overrides"
```

Expected: FAIL(`AssertionError` or `RuntimeError: Retriever not initialized`)。

注:此 sanity test 临时加,在 T4 与其他 `_reset_module_globals` 替换时一起 commit。

- [ ] **Step 3: 改写路由签名**

修改 `rag/ekrs_rag/api/routes/constraints.py`:

(a) 删除模块级全局 + setters(行 33-50):

```python
# 删除 ——
_retriever: Optional[EKRSRetriever] = None


def set_retriever(retriever: EKRSRetriever) -> None:
    """Inject the retriever instance (called at startup)."""
    global _retriever
    _retriever = retriever


_audit_index: Optional[AuditIndex] = None


def set_audit_index(index: AuditIndex) -> None:
    """Inject audit index (called at startup)."""
    global _audit_index
    _audit_index = index
# ——
```

(b) 改路由签名(行 93-98):

```python
@router.post("/constraints", response_model=ConstraintQueryResponse)
async def query_constraints(
    query: ConstraintQuery,
    retriever: EKRSRetriever = Depends(get_retriever),
    audit_index: AuditIndex | None = Depends(get_audit_index),
    _auth: None = Depends(require_parser_token),
) -> ConstraintQueryResponse:
    """... (existing docstring unchanged) ..."""
```

(c) 删 `request: Request` 形参后,(replay 分支内) 删:

```python
        # 删除 ——
        retriever: EKRSRetriever = getattr(request.app.state, "retriever", None) or _retriever
        # ——

        # 替换为
        # (retriever 已从 Depends 注入)
```

(d) 主流程分支(行 186-188):

```python
        # 删除 ——
        retriever: EKRSRetriever = getattr(request.app.state, "retriever", None) or _retriever
        if retriever is None:
            raise RuntimeError("Retriever not initialized")
        # ——

        # 替换为
        # (retriever 形参已通过 Depends 注入)
```

(e) replay 分支 audit_index 检查(行 125-126):

```python
        if audit_index is None:
            raise HTTPException(status_code=503, detail="audit index not initialized")
```
保持不变 — 但现在 `audit_index` 是形参而非 module global。

- [ ] **Step 4: 跑测试 → PASS**

```bash
cd rag && python -m pytest tests/integration/test_query_replay.py -v --tb=short -k "test_constraints_route_uses_dependency_overrides"
cd rag && python -m pytest tests/unit/api/test_route_dependencies.py -v
```

Expected: 5 dep tests 仍 pass;sanity test 仍 FAIL 因为 `test_query_replay.py` 还在用 `set_X` 改全局(TestClient 内的 app `state` 默认空,route 已不读 _retriever 全局但测试 fixture 还没用 overrides 注入)— 见 T4 修复。

(若 sanity test FAIL 原因是 set_audit_index 的 side-effect 残留,可先临时在 sanity test 加 `app.state.audit_index = ...` 占位,后续 T4 一并处理。)

- [ ] **Step 5: 跑 sanity 测试已存在(已有 `test_replay_*` 等)在 _reset_module_globals 仍起作用的过渡期下,验证它们未崩溃**

```bash
cd rag && python -m pytest tests/integration/test_query_replay.py -v --tb=short
```

Expected: `_reset_module_globals` 还在,test 通过。但仍会有 `set_audit_index(audit_setup["idx"])` 调用 — 此调用在 T4 会失败(`set_audit_index` 已删),T4 是它们真正迁移的任务。

- [ ] **Step 6: Commit**

```bash
cd /home/pangzy/code_project/EKRS && git add rag/ekrs_rag/api/routes/constraints.py rag/tests/integration/test_query_replay.py
git commit -m "T2: constraints route uses Depends(get_retriever, get_audit_index); remove 2 globals + 2 setters

- Module-level _retriever/_audit_index and set_retriever/set_audit_index deleted
- Route signature: retriever and audit_index now via Depends (replaces request.app.state.* fallback)
- Replay branch keeps 'if audit_index is None: 503' check (now reads Depends arg)
- Sanity test test_constraints_route_uses_dependency_overrides added
- _reset_module_globals fixture in test_query_replay.py still in place — full migration in T4"
```

---

### Task 3: Ingestion.py 3 路由改 Depends + 删除 globals/setters

**Files:**
- Modify: `rag/ekrs_rag/api/routes/ingestion.py`(删除 3 globals + 3 setters,3 路由签名)

**Interfaces:**
- Consumes: Task 1 提供的 `get_pipeline`/`get_redis_lock`/`get_task_repo`
- Produces: 路由 handler `ingestion_notify`、`ingestion_status`、`ingestion_replay` 通过 Depends 取依赖。模块中 `_pipeline`/`_lock`/`_repo`/`set_pipeline`/`set_redis_lock`/`set_task_repo` 6 标识符全消失。

- [ ] **Step 1: 写 failing 集成测试**

在 `tests/integration/test_ingestion_replay.py` 顶部加(后续 T5 跟同文件其他测试一起迁移):

```python
def test_ingestion_replay_route_uses_dependency_overrides():
    """Ingestion /replay route gets repo via Depends."""
    from ekrs_rag.api.routes.ingestion import router, get_task_repo
    app = FastAPI()
    app.include_router(router)

    sentinel_repo = MagicMock()
    sentinel_repo.get.return_value = None  # task unknown -> 404
    app.dependency_overrides[get_task_repo] = lambda: sentinel_repo

    client = TestClient(app)
    resp = client.post("/v1/ingestion/replay", json={"request_id": "x"}, headers={"X-Parser-Token": ""})
    assert resp.status_code == 404
    sentinel_repo.get.assert_called_with("x")
```

- [ ] **Step 2: 跑测试 → FAIL (route 仍用 _repo 全局)**

```bash
cd rag && python -m pytest tests/integration/test_ingestion_replay.py -v --tb=short -k "test_ingestion_replay_route_uses_dependency_overrides"
```

Expected: FAIL(空 repo global → RuntimeError)。

- [ ] **Step 3: 改写 ingestion.py**

修改 `rag/ekrs_rag/api/routes/ingestion.py`:

(a) 删行 32-53(globals + setters):

```python
# 删除 —— 全部 3 globals + 3 setters
```

(b) 找 `ingestion_notify` handler,改签名加 3 个 Depends:

```python
@router.post("/notify", status_code=202)
async def ingestion_notify(
    body: IngestionNotification,
    request_id: str | None = Header(None, alias="X-Request-Id"),
    pipeline: IngestionPipeline = Depends(get_pipeline),
    lock: RedisLock = Depends(get_redis_lock),
    repo: TaskRepo = Depends(get_task_repo),
    _auth: None = Depends(require_parser_token),
): ...
```

(c) `ingestion_status` 同理(只 repo):

```python
@router.get("/status/{doc_hash}")
async def ingestion_status(
    doc_hash: str,
    repo: TaskRepo = Depends(get_task_repo),
): ...
```

(d) `ingestion_replay` 同理(repo + _auth):

```python
@router.post("/replay", status_code=202)
async def ingestion_replay(
    body: IngestionReplayRequest,
    repo: TaskRepo = Depends(get_task_repo),
    _auth: None = Depends(require_parser_token),
): ...
```

(e) handler 内部,删 `_pipeline`/`_lock`/`_repo` 的所有 `getattr(request.app.state, ..., None) or _x` 行(若存在),直接用形参名。

- [ ] **Step 4: 跑测试 → PASS**

```bash
cd rag && python -m pytest tests/integration/test_ingestion_replay.py -v --tb=short -k "test_ingestion_replay_route_uses_dependency_overrides"
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
cd /home/pangzy/code_project/EKRS && git add rag/ekrs_rag/api/routes/ingestion.py rag/tests/integration/test_ingestion_replay.py
git commit -m "T3: ingestion routes use Depends; remove 3 globals + 3 setters

- ingestion_notify: pipeline, lock, repo via Depends
- ingestion_status/{doc_hash}: repo via Depends
- ingestion_replay: repo via Depends
- Module-level _pipeline/_lock/_repo and set_pipeline/set_redis_lock/set_task_repo deleted
- Existing set_pipeline(mock) call sites will break in T5 — intentional, T5 migrates"
```

---

### Task 4: test_query_replay.py 迁移到 dependency_overrides

**Files:**
- Modify: `rag/tests/integration/test_query_replay.py`(删 `_reset_module_globals` fixture,把 2 个 `set_X(mock)` 改成 `app.dependency_overrides[get_x] = lambda: mock`)

**Interfaces:**
- Consumes: Task 1 `get_retriever`、`get_audit_index`
- Produces: 测试代码不再 import `set_retriever`/`set_audit_index`(这些已删,ImportError 即验证)

- [ ] **Step 1: 验证 `_reset_module_globals` 仅在已知 3 文件中使用 (P1 fix)**

```bash
grep -rn "_reset_module_globals" rag/tests/ --include="*.py"
```

Expected: 仅 `test_query_replay.py`(本任务目标)、`test_ingestion_replay.py`(T5)、`test_ingestion_phase4.py`(T6)。若其他文件出现,HALT 并把该文件加入 T4-T6 迁移清单。

- [ ] **Step 2: 运行当前测试基线 → 期望 FAIL 后(因为 set_X 已删)**

```bash
cd rag && python -m pytest tests/integration/test_query_replay.py -v --tb=short 2>&1 | tail -20
```

Expected: 多个 `ImportError: cannot import name 'set_retriever' from 'ekrs_rag.api.routes.constraints'`(因 Task 2 已删)。

- [ ] **Step 3: 删 `_reset_module_globals` fixture,改用 overrides**

修改 `tests/integration/test_query_replay.py`:

(a) 删 `_reset_module_globals` fixture(imports `constraints as cmod` 的整段,与 `cmod.set_retriever(None)` 与 `set_writer(None)` 重置代码)。可保留 `set_writer(None)` 给 `audit_setup` fixture 用,但不要做 module-level 重置。

(b) 每个测试 body:

```python
# 之前
app = FastAPI()
app.add_middleware(ObservabilityMiddleware)
app.include_router(constraints_router)
from ekrs_rag.api.routes import constraints as cmod
cmod.set_audit_index(audit_setup["idx"])  # or None

# 之后
app = FastAPI()
app.add_middleware(ObservabilityMiddleware)
app.include_router(constraints_router)
app.dependency_overrides[get_retriever] = lambda: mock_retriever()  # or whatever mock
app.dependency_overrides[get_audit_index] = lambda: audit_setup["idx"]  # or some other mock
```

具体 mocking 逻辑保留每个测试原本的语义(读 test 文件,逐 test 迁)。

(c) 删 `import` 中无用的 `from ekrs_rag.api.routes import constraints as cmod`(若不再被引用)。

- [ ] **Step 4: 跑测试 → PASS**

```bash
cd rag && python -m pytest tests/integration/test_query_replay.py -v --tb=short
```

Expected: 所有 5 个 replay 测试通过。`get_retriever` override 让 retriever 是 mock `MockRetriever` (在 `test_constraints_route_uses_dependency_overrides` 等测试中显式设)。

- [ ] **Step 5: Commit**

```bash
cd /home/pangzy/code_project/EKRS && git add rag/tests/integration/test_query_replay.py
git commit -m "T4: test_query_replay.py migrates to app.dependency_overrides

- Delete _reset_module_globals fixture (Task 17 I4 band-aid no longer needed)
- Replace set_retriever/set_audit_index calls with
  app.dependency_overrides[get_X] = lambda: mock
- Each test still creates fresh FastAPI() so no override cross-pollution
- Verified 5 replay tests + sanity test pass"
```

---

### Task 5: test_ingestion_replay.py + test_ingestion.py 迁移

**Files:**
- Modify: `rag/tests/integration/test_ingestion_replay.py`(5 个 `set_pipeline(mock)`/`set_pipeline(None)` 调用改 overrides)
- Modify: `rag/tests/integration/test_ingestion.py`(3 个 set_X 调用,lines 54-56,在 fixture 内) — **P0 fix from gstack-plan-eng-review**: 原 plan 漏列,eng-review 实际核代码发现

**Interfaces:**
- Consumes: Task 1 `get_pipeline`、`get_task_repo`、`get_redis_lock`
- Produces: 所有 `set_X` 调用消失(两个文件)

- [ ] **Step 1: 跑基线 → FAIL (set_X 已删)**

```bash
cd rag && python -m pytest tests/integration/test_ingestion_replay.py tests/integration/test_ingestion.py -v --tb=short 2>&1 | tail -15
```

Expected: 多个 `ImportError`。

- [ ] **Step 2: 改 5 处 set_pipeline 调用 (test_ingestion_replay.py)**

每个 test,模式:

```python
# 之前
imod.set_pipeline(mock)
# 或
imod.set_pipeline(None)

# 之后 (TestClient path)
app = FastAPI()
app.include_router(ingestion_router)
app.dependency_overrides[get_pipeline] = lambda: mock
app.dependency_overrides[get_task_repo] = lambda: repo
# ... etc
client = TestClient(app)
```

具体 mocks 看 test 文件每个 test 的 fixture/上下文(逐 test 读)。

- [ ] **Step 3: 改 test_ingestion.py 3 处 set_X 调用 (lines 54-56)**

读 `/home/pangzy/code_project/EKRS/rag/tests/integration/test_ingestion.py` 找到 fixture/上下文,3 个 set_pipeline/set_redis_lock/set_task_repo 改为 overrides。模式同 T6。

- [ ] **Step 4: 跑测试 → PASS**

```bash
cd rag && python -m pytest tests/integration/test_ingestion_replay.py tests/integration/test_ingestion.py -v --tb=short
```

Expected: 所有测试 pass(2 文件合计)。

- [ ] **Step 5: Commit**

```bash
cd /home/pangzy/code_project/EKRS && git add rag/tests/integration/test_ingestion_replay.py rag/tests/integration/test_ingestion.py
git commit -m "T5: test_ingestion_replay.py + test_ingestion.py migrate to overrides

- test_ingestion_replay.py: 5 set_pipeline calls → dependency_overrides
- test_ingestion.py: 3 set_X calls in fixture (line 54-56) → dependency_overrides
- (test_ingestion.py was added after gstack-plan-eng-review caught the gap)"
```

---

### Task 6: test_ingestion_phase4.py 迁移

**Files:**
- Modify: `rag/tests/integration/test_ingestion_phase4.py`(fixture 内 3 set_X 改 overrides,加 teardown 清 overrides)

- [ ] **Step 1: 跑基线 → FAIL**

```bash
cd rag && python -m pytest tests/integration/test_ingestion_phase4.py -v --tb=short 2>&1 | tail -15
```

- [ ] **Step 2: 改 fixture 内 3 行**

文件内现有 fixture(行 ~73-81):

```python
# 之前
from ekrs_rag.api.routes import ingestion
ingestion.set_pipeline(mock_pipeline)
ingestion.set_redis_lock(lock)
ingestion.set_task_repo(repo)

with TestClient(app) as c:
    yield c, repo, lock, mock_pipeline

# 之后
from ekrs_rag.api.routes.ingestion import (
    get_pipeline, get_redis_lock, get_task_repo,
)
app.dependency_overrides[get_pipeline] = lambda: mock_pipeline
app.dependency_overrides[get_redis_lock] = lambda: lock
app.dependency_overrides[get_task_repo] = lambda: repo

yield TestClient(app), repo, lock, mock_pipeline

# teardown
try:
    yield ...
finally:
    app.dependency_overrides.clear()
```

注:原 fixture 用 `from ekrs_rag.main import app`(生产 app singleton)。生产 app 的 `dependency_overrides` 是 per-app 字段,fixture 末尾必须 clear,否则污染后续测试(虽然此测试是唯一用此 app 的,但安全为先)。

- [ ] **Step 3: 跑测试 → PASS**

```bash
cd rag && python -m pytest tests/integration/test_ingestion_phase4.py -v --tb=short
```

Expected: 全部 ingestion phase4 测试通过。

- [ ] **Step 4: Commit**

```bash
cd /home/pangzy/code_project/EKRS && git add rag/tests/integration/test_ingestion_phase4.py
git commit -m "T6: test_ingestion_phase4.py migrates fixture to overrides + teardown clear"
```

---

### Task 7: main.py lifespan 5 个 set_X → app.state.X

**Files:**
- Modify: `rag/ekrs_rag/main.py`(行 ~111, 118, 128, 129, 162)

- [ ] **Step 1: 跑全 suite baseline → 期望 PASS (任务开始现状)**

```bash
cd rag && python -m pytest tests/ -q --tb=short 2>&1 | tail -5
```

Expected: 320 passed, 1 skipped, 0 failed(T1-T6 已就位)。

- [ ] **Step 2: 改写 lifespan**

`rag/ekrs_rag/main.py`:

(a) 找所有 5 个 `set_X(_X)` 调用(约行 111, 118, 128, 129, 162)。每个改为:

```python
# 之前
constraints.set_retriever(_retriever)

# 之后
app.state.retriever = _retriever
```

完整映射:
- `constraints.set_retriever(_retriever)` → `app.state.retriever = _retriever`
- `ingestion.set_pipeline(_pipeline)` → `app.state.pipeline = _pipeline`
- `ingestion.set_redis_lock(_redis_lock)` → `app.state.redis_lock = _redis_lock`
- `ingestion.set_task_repo(_task_repo)` → `app.state.task_repo = _task_repo`
- `constraints.set_audit_index(_audit_index)` → `app.state.audit_index = _audit_index`

(b) 检查 import: `from .api.routes import constraints, ingestion` 仍然需要(可能模块端 type hint / 其他引用)。如果只因 `set_X` 而 import,检查是否还需要 import,可以保留 import 段让未来加更多路由符号时不破坏。

(c) 跑 sanity: `grep "set_retriever\|set_audit_index\|set_pipeline\|set_redis_lock\|set_task_repo" rag/ -r --include="*.py"` 应输出空。

- [ ] **Step 3: 跑全 suite → PASS**

```bash
cd rag && python -m pytest tests/ -q --tb=short 2>&1 | tail -5
cd rag && python -m pytest tests/ -q --cov=ekrs_rag --cov-report=term -q 2>&1 | tail -3
```

Expected: 320 passed, 1 skipped, 0 failed;coverage ≥ 78% aggregate。

- [ ] **Step 4: Commit**

```bash
cd /home/pangzy/code_project/EKRS && git add rag/ekrs_rag/main.py
git commit -m "T7: main.py lifespan writes app.state.X (no set_X calls)

Replaces 5 constraints.set_X(_X) / ingestion.set_X(_X) calls with:
  app.state.retriever = _retriever
  app.state.audit_index = _audit_index
  app.state.pipeline = _pipeline
  app.state.redis_lock = _redis_lock
  app.state.task_repo = _task_repo

No import changes (constraints and ingestion still imported for other symbols)."
```

---

### Task 8: Final 验收 + 完整测试 + grep 验证 + tag

**Files:**
- 不改代码,只跑验收 grep / 测试 / 打 tag

- [ ] **Step 1: 验收 grep**

```bash
cd /home/pangzy/code_project/EKRS
grep -rE "^_(retriever|audit_index|pipeline|redis_lock|repo)\s*:" rag/ekrs_rag/api/routes/ && echo "FOUND globals" || echo "OK: no module globals"
grep -rE "def set_(retriever|audit_index|pipeline|redis_lock|task_repo)\b" rag/ --include="*.py" && echo "FOUND setters" || echo "OK: no setters"
```

Expected: 两个 grep 均"OK"。

- [ ] **Step 2: Import 失败验证**

```bash
python -c "from ekrs_rag.api.routes.constraints import set_retriever" 2>&1 | tail -2
python -c "from ekrs_rag.api.routes.ingestion import set_pipeline" 2>&1 | tail -2
```

Expected: 两个都 `ImportError`。

- [ ] **Step 3: 跑全套件**

```bash
cd rag && python -m pytest tests/ -v --tb=short --no-header 2>&1 | grep -E "passed|failed" | tail -3
```

Expected: 320 passed, 1 skipped, 0 failed。

- [ ] **Step 4: 跑覆盖率**

```bash
cd rag && python -m pytest tests/ -q --cov=ekrs_rag --cov-report=term 2>&1 | tail -3
```

Expected: aggregate ≥ 78%;Phase 5 modules ≥ 87%。

- [ ] **Step 5: 更新 ledger + 打 tag**

```bash
cd /home/pangzy/code_project/EKRS
echo "## Phase 5.5 E — retriever → FastAPI Depends — DONE

Range: <COMMIT_BASE>..<COMMIT_HEAD>
8 commits (T1-T8)
320 passed, 1 skipped, 0 failed
Coverage: ≥ 78% aggregate, Phase 5 modules ≥ 87%

All spec §验收 checks pass:
- grep no module globals
- grep no setters
- import set_retriever raises ImportError
- Iron Rules unchanged
- 15 audit events unchanged
- Phase 5 replay behavior unchanged
" >> .superpowers/sdd/PHASE-5.5-E-COMPLETE.md

git tag -a phase5.5-e-retriever-depends -m "Phase 5.5 E: 5 module globals → FastAPI Depends + app.state pattern

Range: T1-T8 (8 commits)
Removed: set_X(5) + _reset_module_globals fixture
Added: get_X(5) deps + 5 unit tests
Tests: 320 passed, 1 skipped, 0 failed

Design: docs/superpowers/specs/2026-07-13-phase5.5-retriever-depends-design.md
Plan: docs/superpowers/plans/2026-07-13-phase5.5-retriever-depends.md"
git tag -n3 phase5.5-e-retriever-depends | head -5
```

- [ ] **Step 6: Commit ledger + (可选) push tag**

```bash
git add .superpowers/sdd/PHASE-5.5-E-COMPLETE.md
git commit -m "T8: phase 5.5 E complete ledger + tag phase5.5-e-retriever-depends"
```

---

## Augmentations (gstack-plan-eng-review)

**Reviewer:** gstack-plan-eng-review skill (2026-07-13)
**Verdict:** APPROVE_WITH_PATCHES (2 P1 patched inline, 2 P2 / 2 P3 noted)

### Findings

| # | Sev | Conf | Location | Issue | Status |
|---|-----|------|----------|-------|--------|
| F1 | P1 | 9/10 | T1 Step 1 `_mock_request` | Uses `MagicMock` for `request.app.state` — MagicMock auto-creates `state.retriever` as a truthy object, so `getattr(state, "retriever", None)` returns a truthy Mock, `r is None` is False, and the 503 check is bypassed. All 5 contract tests would FAIL with "DID NOT RAISE". | **PATCHED**: T1 Step 1 now uses `starlette.datastructures.State()` for `req.app.state` — real attribute container, missing attrs raise `AttributeError`, `getattr(..., None)` returns None. |
| F2 | P1 | 7/10 | T4 deletion of `_reset_module_globals` | Plan assumed `_reset_module_globals` only in 3 known test files. If used elsewhere (e.g., test_healthz.py), silent breakage. | **PATCHED**: T4 Step 1 now pre-verifies with `grep -rn "_reset_module_globals" rag/tests/`. HALT if any other file found. |
| F3 | P0 | 10/10 | File inventory + T5 | **Plan omitted 4th test file `test_ingestion.py` which also uses setters (lines 54-56)**. Found by post-review grep; would have caused silent test breakage after T7 (set_X deleted but test still imports/calls). | **PATCHED**: File inventory + T5 now include test_ingestion.py. |
| F4 | P2 | 6/10 | T1 tests | No explicit regression test for behavior change `RuntimeError (500) → HTTPException(503)` on uninitialized deps. Real but already covered by `exc.value.status_code == 503` assertion. | **NOTED**: T1 tests assert `status_code == 503` (not 500), which IS the regression check. Sufficient. |
| F5 | P2 | 5/10 | T8 verification grep | `set_X` function removal verified, but orphan `set_X` imports in test files not checked. | **NOTED**: T8 Step 1 grep should add `grep -rE "from ekrs_rag\.api\.routes.*set_" rag/tests/`. Implementer may add to T8 brief. |
| F6 | P3 | 4/10 | T7 Step 3 coverage check | Plan verifies aggregate ≥ 78% but not per-module Phase 5 modules ≥ 87%. | **NOTED**: T7 Step 3 should add `python -m coverage report --include="ekrs_rag/api/routes/*" -m`. Implementer may extend. |
| F7 | P3 | 4/10 | Plan structure | Plan lacks ASCII data-flow diagram (per gstack convention for non-trivial data flow). Spec has one — reference is enough. | **NOTED**: Spec §数据流 covers this. No plan change. |
| F8 | P3 | 5/10 | main.py current state | main.py ALREADY has `app.state.X` writes for retriever/redis_lock/task_repo/audit_index alongside `set_X()` calls. Only `pipeline` is set_X-only. Plan T7 says "5 set_X → app.state.X" but reality is 5 set_X removed + 1 new app.state.pipeline write. | **NOTED**: T7 brief should reflect actual state. No plan change. |

### Strengths

- D1-D5 design decisions are sound; the 4 vs 1 strict/optional split maps cleanly to the route semantics.
- T1 contract tests are correct in intent — the bug was infrastructure (MagicMock), not test design.
- T6's `try/finally: app.dependency_overrides.clear()` handles the production-app-singleton asymmetry correctly.
- Hard delete (no BC shim) per D3 is the right call for 5 small deps + 3 known test files.

### Review log

```bash
~/.claude/skills/gstack/bin/gstack-review-log '{"skill":"plan-eng-review","timestamp":"2026-07-13T20:50:00+08:00","status":"clean","unresolved":0,"critical_gaps":0,"issues_found":8,"mode":"FULL_REVIEW","commit":"'"$(git rev-parse --short HEAD)"'"}'
```

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR | 8 issues found, 1 P0 + 2 P1 patched inline, 2 P2 + 3 P3 noted |

**VERDICT:** Eng Review CLEARED — ready to implement

---

## Self-Review (writing-plans 清单)

**Spec 覆盖:**
- D1 改 5 个全局:Tasks 1-7 ✓
- D2 用 app.dependency_overrides:Tasks 4-6 ✓
- D3 硬删除 set_X + fixture:Tasks 2-3 + 4 ✓
- D4 get_audit_index Optional:Task 1 ✓
- D5 503:Task 1 (单测) + 1-7 (deps 实现) ✓
- main.py 改 app.state:Task 7 ✓
- 测试迁移:Tasks 4-6 ✓
- 5 新单测:Task 1 ✓
- Acceptance grep + import + 测试:Task 8 ✓

**占位符扫描:** 全文 grep "TBD/TODO/FIXME/待补"= 0 命中。

**类型一致性:**
- `get_retriever(request) -> EKRSRetriever` (Task 1) ↔ `retriever: EKRSRetriever = Depends(get_retriever)` (Task 2) ✓
- `get_audit_index(request) -> AuditIndex | None` (Task 1) ↔ `audit_index: AuditIndex | None = Depends(get_audit_index)` (Task 2) ✓
- 5 个 deps 的 `app.state.X` 名称 ↔ main.py `app.state.X = _X` (Task 7) ✓
- `set_X` 名称 ↔ 各测试文件的 from-import 与全局清理(已删)(Tasks 4-6) ✓
- `routes/constraints.py` 行号指引基于新 spec 落地的预估(Task 2 步骤内联 code blocks 不依赖文件行号)

无 plan-internal 一致性问题。

---

## 执行 Handoff(Phase 5.5 E plan 已落档,不立即执行)

Plan 落档: `docs/superpowers/plans/2026-07-13-phase5.5-retriever-depends.md`
Spec 同步: `docs/superpowers/specs/2026-07-13-phase5.5-retriever-depends-design.md`

按 user 选:"生成 plan 但不立即实施"。执行触发词:用户重启时给"继续"或具体 task 编号。
