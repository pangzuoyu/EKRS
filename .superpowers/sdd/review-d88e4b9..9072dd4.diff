# Review package: d88e4b9..HEAD

## Commits
9072dd4 chore: add fakeredis for test isolation

## Files changed
 rag/pyproject.toml | 1 +
 1 file changed, 1 insertion(+)

## Diff
diff --git a/rag/pyproject.toml b/rag/pyproject.toml
index 95dc07b..6e83cb4 100644
--- a/rag/pyproject.toml
+++ b/rag/pyproject.toml
@@ -22,15 +22,16 @@ build-backend = "setuptools.build_meta"
 
 [tool.setuptools.packages.find]
 include = ["ekrs_rag*"]
 
 [project.optional-dependencies]
 dev = [
     "pytest>=8.0",
     "pytest-asyncio>=0.23",
     "pytest-cov",
     "httpx",  # for TestClient async
+    "fakeredis>=2.20",
 ]
 
 [tool.pytest.ini_options]
 asyncio_mode = "auto"
 testpaths = ["tests"]
